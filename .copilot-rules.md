# Copilot Pro Codebase Rules

## Scope
- Only monitor and manage code within the `/lib` folder
- Ignore all other directories (test/, build/, android/, ios/, etc.)

## Rules
- Focus exclusively on Dart files in lib/
- Do not suggest changes outside lib/
- Consider lib/ as the root of the codebase for analysis